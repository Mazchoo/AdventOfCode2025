#include <iostream>
#include <iomanip>
#include "mod.h"

// Main function for testing and debugging the math functions natively
int main() {

    std::cout << std::endl;
    std::cout << "Press Enter to exit...";
    std::cin.get();

    return 0;
}